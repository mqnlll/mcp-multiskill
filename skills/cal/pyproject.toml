[project]
name = "cal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp-multiskill",
]

[tool.uv.sources]
mcp-multiskill = { path = "../../", editable = true }
