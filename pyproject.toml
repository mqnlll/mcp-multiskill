[project]
name = "mcp-multiskill"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"

[project.optional-dependencies]
full = [
    "argparse-to-json>=0.0.1",
    "mcp[cli]>=1.26.0",
]
skill-need = [
    "argparse-to-json>=0.0.1",
]
